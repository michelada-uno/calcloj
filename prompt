I think persistence/multi-tenant is more important now. I guess we can use `spindel`\'s execution context\'s serialisation. But need to pay attention that in future we also have to save cell\'s style and format information. Moreover that information also could be reactive (at least style... e.g. big number -> red, low number -> green; that is about future but we have to put now ability for such extensions). Looks like that information have to be not in separate data object, but also in spins.
